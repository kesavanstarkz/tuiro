# Use your computer's LAN IP when testing on a physical phone or Expo Go.
# Find your IP on macOS using: ifconfig | grep "inet "
# Or if testing on web / iOS simulator only: http://127.0.0.1:8000/api/v1
EXPO_PUBLIC_API_URL=http://192.168.1.9:8000/api/v1
